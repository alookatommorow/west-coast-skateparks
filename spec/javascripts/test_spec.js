describe('Order form errors', function() {
  beforeEach(function() {
    MagicLamp.load("skateparks/show");
  });

  it('depends on the dom', function() {
    //we know this is rendering fixture but not loading event listeners from application.js
  });
});