var response = {addVisitButton: "<%= escape_javascript (render partial: 'update.html.erb', layout: false )%>",
}

$('.visit-container form').replaceWith(response.addVisitButton);

if ($('div').hasClass('initial-rate-container')) {
  $('.initial-rate-container, .initial-review-container').remove();
}

$('.rate-button-container, .review-button-container').remove();
