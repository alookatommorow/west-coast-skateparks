<%= render partial: "header" %>
<!-- List of posts -->
<% @sorted_posts.each do |post| %>
  <div>
    <%= link_to post.title, buttercms_post_path(post.slug) %>
  </div>
<% end %>

<!-- Pagination links -->
<div>
  <% if @previous_page %>
    <%= link_to "Prev", buttercms_blog_path(:page => @previous_page) %>
  <% end %>

  <% if @next_page %>
    <%= link_to "Next", buttercms_blog_path(:page => @next_page) %>
  <% end %>
</div>