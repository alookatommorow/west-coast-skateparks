var response = {form_class: "add-visit", button_class: "ui primary button add-visit-button", action: "/users/<%= current_user.id %>/skateparks/<%= params[:id] %>/add_visit", text: "Been Here"}

$('.remove-visit-button').attr({
        value: response.text,
        class: response.button_class
      });

$('.visit-container').children().first().attr({
        action: response.action,
      });

$('.initial-rate-container').remove();
$('.initial-review-container').remove();
$('.edit_user_skatepark').remove();

$('.ui.primary.button.review-button').remove();
$('.ui.primary.button.rate-button').remove();