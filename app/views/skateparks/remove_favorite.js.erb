var response = {
  addFavoriteButton: "<%= escape_javascript (render partial: 'remove_favorite.html.erb',layout: false )%>"
}
$('.fav-container form').remove();
$('.fav-container').append(response.addFavoriteButton);